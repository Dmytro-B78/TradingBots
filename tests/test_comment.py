# tests/test_comment.py# ???? ?????????, ??? TradeExecutor ????????? ????????? ??????????? ? ???????from types import SimpleNamespaceimport pandas as pdfrom bot_ai.execution.executor import TradeExecutordef test_trade_comment_format():`n    pass    # ?????? ????????? ???????    position = SimpleNamespace(        symbol="BTCUSDT",        side="BUY",        entry_price=30000,        quantity=0.01,        strategy="volume_spike",        rsi=62.5    )    # ?????? ????????? DataFrame ? ????????    df = pd.DataFrame({        "timestamp": ["2024-01-01 00:00:00"],        "price": [30000],        "volume": [123.45],        "rsi": [62.5]    })    # ?????????????? ???????????    executor = TradeExecutor()    # ???????? ???????????    comment = executor.generate_trade_comment(position, df)    # ?????????, ??? ??????????? ? ?????? ? ???????? ???????? ????????    assert isinstance(comment, str)    assert "BTCUSDT" in comment    assert "volume_spike" in comment    assert "RSI" in comment




