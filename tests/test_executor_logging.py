# tests/test_executor_logging.py# ???? ?????????, ??? TradeExecutor ????????? ???????? ?????? ? CSV-????import csvimport uuidfrom pathlib import Pathfrom typing import List, Dictfrom bot_ai.execution.executor import TradeExecutordef test_trade_logging_to_csv(tmp_path: Path):`n    pass    # ?????? ????????? CSV-????    log_file = tmp_path / f"trades_{uuid.uuid4().hex}.csv"    # ?????? ?????? ??????    trade_data = {        "symbol": "BTCUSDT",        "side": "BUY",        "entry_price": 30000,        "quantity": 0.01,        "strategy": "volume_spike",        "rsi": 62.5    }    # ?????????????? ???????????    executor = TradeExecutor()    # ???????? ??????    executor.log_trade(trade_data, log_file)    # ?????????, ??? ???? ?????? ? ???????? ?????????? ??????    assert log_file.exists()    with log_file.open("r", newline="") as f:        reader = csv.DictReader(f)        rows = list(reader)    assert len(rows) == 1    assert rows[0]["symbol"] == "BTCUSDT"    assert rows[0]["strategy"] == "volume_spike"




